# Dev: Web + Vanilla js

  - Don't use React or any other framework. Just plain old HTML, CSS and vanilla JS.
  - Locally, for development, init an npm package, a script `dev`, which runs http-server. Server port is 8080.
